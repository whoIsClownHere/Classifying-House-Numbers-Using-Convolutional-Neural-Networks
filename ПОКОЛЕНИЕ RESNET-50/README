
Поколение нейросетей ,где пытаемся использовать модель RESNET50 в различных вариациях 

model.compile(optimizer='adam', loss='categorical_crossentropy', metrics=['accuracy'])

0) RESNET50 - является сверточной нейронной сетью, которой имеет 50 слоёв глубины. 
1) RESNET50_1 - реализация RESNET50 с точностью 0,91
2) RESNET50_2 - добавили Dropout(0.5) только в Residual блоки, при этом точность стала равна 0,892
3) RESNET50_3 - добавили Dropout(0.4) во все места после BatchNormalization(), при этом точность стала равна 0,93426





...Ансамбль нейросетей???
